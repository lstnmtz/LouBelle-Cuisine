import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.LouBelleCuisine.app',
  appName: 'LouBelle Cuisine',
  webDir: 'dist'
};

export default config;
